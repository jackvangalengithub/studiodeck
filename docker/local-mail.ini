; Mounted only by the local Compose override, for both web and worker.
; Fixed SMTP destination: recipient addresses never select an external server.
sendmail_path = "/usr/bin/msmtp --host=mailcatcher --port=1025 --tls=off --auth=off --read-envelope-from --timeout=10 -t"
