const esc=value=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function destinationPage(data,brand){
 const logo=(src,name)=>src?`<img class="destination-logo" src="${esc(src)}" alt="${esc(name)} logo">`:`<span class="destination-monogram" aria-hidden="true">${esc(name.slice(0,1).toUpperCase())}</span>`;
 return `<div class="destination-shell"><header>${brand}<button type="button" class="button small" data-action="account-menu">Your account</button></header><main id="main"><h1>Where would you like to go?</h1><p class="muted">Choose your workspace or a project shared with you.</p>${data.studios.length?`<section><h2>Your studios</h2><div class="destination-grid">${data.studios.map(s=>`<button type="button" class="destination-studio" data-action="destination-studio" data-id="${esc(s.id)}">${logo(s.logo,s.name)}<span><strong>${esc(s.name)}</strong><small>${s.role==='admin'?'Studio admin':'Studio member'}</small></span><span aria-hidden="true">→</span></button>`).join('')}</div></section>`:''}${data.projects.length?`<section><h2>Shared with you</h2><div class="destination-grid">${data.projects.map(p=>`<article class="destination-project" data-destination-project="${esc(p.id)}"><button type="button" class="destination-cover" data-action="destination-client" data-id="${esc(p.id)}" aria-label="Open ${esc(p.name)} as client">${p.has_cover?`<img src="api.php?${esc(new URLSearchParams({action:'destination_cover',project_id:p.id,iteration:p.iteration_id}))}" alt="" loading="lazy">`:'<span aria-hidden="true">'+esc(p.name.slice(0,1))+'</span>'}</button><div class="destination-project-copy"><div class="destination-studio-name">${logo(p.studio_logo,p.studio_name)}<span>${esc(p.studio_name)}</span></div><h3>${esc(p.name)}</h3><p>Iteration ${String(p.iteration_number).padStart(2,'0')}${p.iteration_title?' · '+esc(p.iteration_title):''}</p><div class="row wrap"><button type="button" class="button primary small" data-action="destination-client" data-id="${esc(p.id)}">${p.studio_access?'View as client':'Open presentation'}</button>${p.studio_access?`<button type="button" class="button small" data-action="destination-studio" data-id="${esc(p.studio_id)}" data-project="${esc(p.id)}">Open in studio</button>`:''}</div></div></article>`).join('')}</div></section>`:''}${!data.studios.length&&!data.projects.length?'<div class="notice">No active projects have been shared with this account. Your studio can invite you using your sign-in email.</div>':''}</main></div>`;
}
export function readClientRoute(location){const match=location.pathname.match(/^\/client\/projects\/([A-Za-z0-9_-]+)\/?$/);if(!match)return null;const query=new URLSearchParams(location.search);return {projectId:match[1],iteration:query.get('iteration'),slide:query.get('slide')};}
export function clientProjectUrl(project,iteration,slide){const query=new URLSearchParams();if(iteration)query.set('iteration',iteration);if(slide)query.set('slide',slide);return '/client/projects/'+encodeURIComponent(project)+(query.size?'?'+query:'');}
