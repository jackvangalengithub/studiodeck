export function websiteUi({state,api,esc,button,openModal,closeModal,render,toast,resourceHeaders}) {
 let data=null,studio='',tab='chat',dirty=false,busy=false,message='',source=null,portrait='',pendingPrompt='',galleryOpen=false,previewTemplate='',sourceFile='index.html',previewChannel='',previewReport=null;
 const freeze=value=>document.querySelectorAll('.workspace,.website-workspace,.modal').forEach(el=>{el.inert=value;el.setAttribute('aria-busy',String(value));});
 const copy=v=>structuredClone(v),uid=()=>crypto.randomUUID().replaceAll('-','');
 const btn=(label,act,kind='',extra='')=>button(label,'website-'+act,kind,extra);
 const field=(label,name,value,max=160,area=false)=>`<label>${label}${area?`<textarea rows="4" name="${name}" maxlength="${max}">${esc(value)}</textarea>`:`<input name="${name}" value="${esc(value)}" maxlength="${max}">`}</label>`;
 const footer=label=>`<div class="modal-footer">${button('Cancel','close-modal','ghost')}<button class="button primary" type="submit">${label}</button></div>`;
 const endpoint=(action,params={})=>'/api.php?'+new URLSearchParams({action,website_studio:state.studio.id,...params});
 const asset=id=>endpoint('website_asset',{id});
 async function load(){const sid=state.studio.id;const result=await api('website');if(state.studio.id!==sid)return;data=result;studio=sid;dirty=false;if(new URLSearchParams(location.search).get('website_checkout')==='success'){message='Confirming your payment…';data=await api('website_refresh_billing');message=data.billing.active?'Website activated. You can now publish.':'Payment is processing. Use Check payment to refresh.';}}
 function accept(result){if(result.studio_id!==state.studio.id)return;data=result;dirty=false;render();}
 async function save(){if(!dirty)return;accept(await api('website_save',{draft:data.draft,revision:data.revision}));}
 function page(){
  if(!data||studio!==state.studio.id)return '<p>Opening your website…</p>';
  const d=data.draft;previewChannel=uid();previewReport=null;
  if(!d.started)return welcome();
  return `<div class="website-workspace" ${busy?'inert aria-busy="true"':''}><div class="page-heading"><div><p class="website-eyebrow">STUDIODECK WEBSITE · $39 / MONTH</p><h1>Your work. Your website.</h1><p class="muted">Create once. Keep it current in minutes. Publish only when you’re ready.</p></div><div class="row wrap">${btn('Start from scratch','reset','ghost')}${btn('Undo','undo','ghost',data.can_undo?'':'disabled')}${btn('Save draft','save','',dirty?'':'disabled')}${btn(data.live?'Publish changes':'Publish website','publish','primary')}</div></div><div class="website-status" role="status"><span>${dirty?'Unsaved changes':data.live?'Draft saved · Live website changes only when you publish':'Draft saved · Your website is not published yet'}</span>${data.live?`<a href="${esc(data.url)}" target="_blank" rel="noopener">View live website ↗</a>`:''}</div><div class="website-layout"><aside class="website-panel"><nav class="website-tabs" aria-label="Website editor">${['chat','projects','testimonials','source','publish'].map(t=>btn(t[0].toUpperCase()+t.slice(1),'tab',tab===t?'active':'',`data-tab="${t}" aria-pressed="${tab===t}"`)).join('')}</nav><div class="website-controls">${tab==='chat'?chatPanel(d):tab==='projects'?projects(d):tab==='testimonials'?testimonials(d):tab==='source'?sourcePanel(d):publishing()}</div></aside><section class="website-preview"><div class="row"><strong>Website preview</strong><span class="tag">${dirty?'Save draft to refresh':'Private draft'}</span>${btn('Desktop','device','small ghost','data-size="desktop"')}${btn('Mobile','device','small ghost','data-size="mobile"')}</div><iframe title="Your website draft" sandbox="allow-scripts allow-popups" src="${esc(endpoint('website_preview',{v:data.revision,channel:previewChannel}))}"></iframe><p class="website-preview-health" role="status">Checking preview…</p></section></div></div>`;
 }
 function welcome(){return `<section class="website-welcome"><div class="website-welcome-copy"><p class="website-eyebrow">YOUR STUDIO, OUT IN THE WORLD</p><h1>Good work deserves<br>a beautiful home.</h1><p>Start with a design you love. Make it entirely your own, simply by describing what you have in mind.</p><div class="row">${btn('Find your starting point ↗','gallery','primary')}${btn('Start with a blank page','blank','ghost')}</div><span>Ten considered designs. One page. Yours to change.</span><div class="website-welcome-steps"><div><b>01</b><strong>Choose a feeling</strong><p>Explore full-screen examples.</p></div><div><b>02</b><strong>Make it yours</strong><p>Add your work and talk through changes.</p></div><div><b>03</b><strong>Share it with the world</strong><p>Preview, then publish when ready.</p></div></div></div><div class="website-welcome-art" aria-hidden="true"><div class="website-cover-frame"><small>ATELIER / INTERIORS</small><h2>A sense<br>of place.</h2><img src="/assets/interior.webp" alt=""><span>Considered spaces. Everyday living.</span></div><div class="website-cover-note">Your story.<br>Your point of view.</div></div></section>`;}
 function chatPanel(d){return `<section class="website-chat"><div class="row"><h2>Make it entirely yours</h2>${btn('Starting designs','gallery','small ghost')}</div><p>Describe any change: a new layout, your updated address, an image gallery, or a project you’d like to add.</p><div class="website-conversation">${(d.conversation||[]).map(m=>`<div class="website-chat-bubble ${m.role}"><small>${m.role==='user'?'You':'Website assistant'}</small><p>${esc(m.text)}</p></div>`).join('')}</div><form data-form="website-chat"><label for="website-prompt">What would you like to change?</label><textarea id="website-prompt" name="prompt" rows="5" maxlength="4000" placeholder="Make this feel like an architectural magazine…" ${data.ai?'':'disabled'} required></textarea><button class="button primary" type="submit" ${data.ai?'':'disabled'}>Update my draft ↗</button><small>${data.ai?data.ai_remaining+' edits left this month':'AI is not connected. You can edit the files in Source.'}</small></form><div class="website-prompt-ideas">${['Make the layout more editorial','Add a project from StudioDeck','Update my studio address'].map(p=>btn(esc(p),'prompt','small ghost',`data-prompt="${esc(p)}"`)).join('')}</div><p class="website-chat-message" role="status">${esc(message)}</p></section>`;}
 function sourcePanel(d){return `<h2>Your website files</h2><p>Edit or paste files from another designer or LLM. These are the source of your website; saving never reapplies a template.</p><div class="row wrap">${Object.keys(d.files).map(f=>btn(f,'file','small '+(sourceFile===f?'primary':'ghost'),`data-file="${f}"`)).join('')}</div><label class="website-source-label">${sourceFile}<textarea class="website-code" data-website-file="${sourceFile}" spellcheck="false" aria-label="${sourceFile} source">${esc(d.files[sourceFile])}</textarea></label><div class="row wrap">${btn('Save & preview','save','primary')}${btn('Copy file','copy-source','ghost')}</div><label class="website-file-import">Replace files<input type="file" data-website-files multiple accept=".html,.css,.js"></label><details><summary>Image library</summary><p>Use these paths in your HTML or CSS. Images are optimized when published.</p>${[...new Set([d.hero_asset,d.logo,...d.projects.flatMap(p=>p.images.map(im=>im.asset)),...d.testimonials.map(t=>t.photo)].filter(Boolean))].map(id=>`<div class="website-asset-reference"><img src="${esc(asset(id))}" alt=""><code>assets/${id}.webp</code></div>`).join('')}</details><div class="website-source-checks">${[...(data.checks?.errors||[]),...(data.checks?.warnings||[])].map(m=>`<p>${esc(m)}</p>`).join('')}</div>`;}
 function renderGallery(){document.getElementById('website-gallery')?.remove();if(!galleryOpen)return;const selected=data.templates.find(t=>t.id===previewTemplate);const node=document.createElement('div');node.id='website-gallery';node.className='website-gallery-overlay';node.setAttribute('role','dialog');node.setAttribute('aria-modal','true');node.setAttribute('aria-label',selected?selected.name+' full-screen preview':'Choose your starting point');node.innerHTML=selected?`<header><div><strong>${esc(selected.name)}</strong><span>${esc(selected.description)} · Example content</span></div><div class="row">${btn('All designs','gallery-back','ghost')}${btn('Use this starting point','choose-template','primary',`data-template="${selected.id}"`)}${btn('Close','gallery-close','ghost')}</div></header><iframe class="website-full-example" title="${esc(selected.name)} full-screen example" sandbox="allow-scripts" src="${esc(endpoint('website_template_preview',{template:selected.id}))}"></iframe>`:`<header><div><p class="website-eyebrow">TEN WAYS TO BEGIN</p><h2>Find your starting point.</h2><p>Every design is a single page. Every detail can change.</p></div>${btn('Close','gallery-close','ghost')}</header><div class="website-template-grid">${data.templates.map(t=>`<article class="website-example-card ${t.tone}"><button class="website-example-preview" data-action="website-example" data-template="${t.id}" aria-label="Preview ${esc(t.name)} full screen"><iframe title="${esc(t.name)} thumbnail" tabindex="-1" loading="lazy" sandbox="" src="${esc(endpoint('website_template_preview',{template:t.id}))}"></iframe><span>Explore full screen ↗</span></button><div class="website-example-caption"><div><h3>${esc(t.name)}</h3><p>${esc(t.description)}</p></div>${btn('Choose','choose-template','small',`data-template="${t.id}"`)}</div></article>`).join('')}</div>`;document.body.append(node);node.querySelector('button')?.focus();}
 async function chooseTemplate(id){await save();galleryOpen=false;renderGallery();if(data.draft.started){openModal('Use a new starting point?',`<p>This replaces the draft design and keeps your project materials. Your current live website stays unchanged. You can undo this choice.</p><div class="modal-footer">${button('Keep my design','close-modal','ghost')}${btn('Replace draft design','start','primary',`data-template="${id}"`)}</div>`);}else{accept(await api('website_start',{template:id,revision:data.revision}));tab='chat';render();}}
 async function checkViewport(width){return new Promise(resolve=>{const channel=uid(),frame=document.createElement('iframe');frame.sandbox='allow-scripts';frame.style.cssText=`position:fixed;left:-20000px;top:0;width:${width}px;height:900px;border:0`;frame.title='Website publication check';let timer;const done=result=>{clearTimeout(timer);window.removeEventListener('message',receive);frame.remove();resolve({...result,width});};const receive=e=>{if(e.source===frame.contentWindow&&e.data?.type==='website-preview-report'&&e.data.channel===channel)done(e.data);};window.addEventListener('message',receive);timer=setTimeout(()=>done({errors:['Preview checks timed out. Please try again.']}),12000);frame.src=endpoint('website_preview',{v:data.revision,channel});document.body.append(frame);});}
 function projects(d){return `<h2>Your portfolio</h2><p>Choose exactly what visitors see. These copies stay here even if the original project is deleted.</p>${btn('Add a project','choose-project','primary')}<div class="website-items">${d.projects.map(p=>`<article class="website-item"><div>${p.images[0]?`<img src="${esc(asset(p.images[0].asset))}" alt="" loading="lazy">`:''}<strong>${esc(p.title)}</strong><small>${p.included?'Included in draft':'Hidden from next publish'}</small></div><div>${btn('Edit','edit-project','small',`data-id="${p.id}"`)}${btn(p.included?'Hide':'Include','toggle-project','small ghost',`data-id="${p.id}"`)}${data.projects.some(s=>s.id===p.source_id)?btn('Update from project','import','small ghost',`data-id="${p.source_id}"`):'<small>Source project removed · Website copy retained</small>'}</div></article>`).join('')||'<p class="muted">Your portfolio starts with your first selected project.</p>'}</div>`;}
 function testimonials(d){return `<h2>Let your clients speak</h2><p>Add approved quotes and choose where they appear.</p>${btn('Add a testimonial','testimonial','primary')}<div class="website-items">${d.testimonials.map(t=>`<article class="website-item"><div><strong>${esc(t.name)}</strong><p>${esc(t.content)}</p><small>${t.approved?'Approved for publication':'Private draft'}</small></div>${btn('Edit','testimonial','small',`data-id="${t.id}"`)}${btn('Remove','remove-testimonial','small ghost',`data-id="${t.id}"`)}</article>`).join('')}</div>`;}
 function publishing(){const b=data.billing,domain=data.domain;return `<h2>Ready when you are</h2><p>${b.local?'Local preview mode · Publishing is enabled for development.':b.active?'Your Website subscription is active.':'Build and preview for free. Publish for $39 USD/month, plus applicable tax.'}</p><p>Includes hosting, ten starting designs, projects, testimonials, image optimization, search essentials, and 50 AI edits per month.</p>${!b.active?btn(b.available?'Activate Website · $39/month':'Payments not configured','checkout','primary',b.available?'':'disabled'):''}${btn('Check payment','refresh-billing','small ghost')}${b.has_subscription?button('Manage subscription','billing','small ghost'):''}<h3>Your website address</h3><p class="website-url">${esc(data.url)}</p><p class="form-hint">${!data.live?'This address is reserved. Publish your website to make it available here.':!b.active?'Your website is paused until its subscription is active.':'Your published website is available at this address.'}</p><form data-form="website-domain">${field('Connect your domain','name',domain.name,253)}<p class="form-hint">Use a www address. Keep your email DNS records unchanged.</p><button class="button" type="submit">${domain.name?'Check connection':'Set domain'}</button></form>${domain.name?`<div class="website-dns"><p>${domain.verified?'Ownership verified. Publish to use this domain.':'Add these DNS records, then check connection.'}</p><dl><dt>CNAME · ${esc(domain.name)}</dt><dd>${esc(domain.target||'Hosting must be configured by the operator first')}</dd><dt>TXT · _studiodeck.${esc(domain.name)}</dt><dd>${esc(domain.token)}</dd></dl><p class="form-hint">DNS can take time to update. HTTPS is handled by the configured hosting service.</p></div>`:''}<h3>Published versions</h3>${data.live?`<a class="button" href="${esc(endpoint('website_export'))}">Download static website (.zip)</a>`:''}<p class="form-hint">Restore loads an earlier version into your draft. Preview and publish to make it live.</p>${data.releases.map(r=>`<div class="website-item"><div><strong>${esc(new Date(r.created_at).toLocaleString())}</strong><small>${r.id===data.live?.release?'Current live version':'Earlier version'}</small></div>${btn('Restore to draft','restore','small ghost',`data-id="${r.id}"`)}</div>`).join('')||'<p>No published versions yet.</p>'}`;}
 async function importModal(pid,prompt=''){pendingPrompt=prompt;
  await save();source=await api('website_sources',{project_id:pid});const previous=data.draft.projects.find(p=>p.source_id===pid);
  openModal(previous?'Update public project copy':'Add project to website',`<form data-form="website-import"><p>Review the public copy and select photographs. Private documents, budgets, and client details are never included automatically.</p>${field('Public title','title',source.project.title,160)}${field('Public description','description',source.project.description,6000,true)}<p class="form-hint">Updating replaces this website project’s title, description, and selected gallery. Its page anchor and visibility are kept.</p><div class="website-source-grid">${source.images.map(im=>`<label class="website-source"><img loading="lazy" src="${esc(endpoint('website_source_image',{project_id:pid,id:im.id}))}" alt=""><span><input type="checkbox" name="image" value="${im.id}"> ${esc(im.title||'Project image')}</span><input name="alt_${im.id}" maxlength="250" placeholder="Describe this image for visitors" aria-label="Image description for ${esc(im.title)}" value="${esc(im.title)}"></label>`).join('')||'<p>No project photographs available. You can add images after importing.</p>'}</div>${footer(previous?'Update draft copy':'Add to draft')}</form>`,true);

 }
 function editProject(id){const p=data.draft.projects.find(p=>p.id===id);openModal('Website project',`<form data-form="website-project" data-id="${id}">${field('Public title','title',p.title,160)}${field('Public description','description',p.description,6000,true)}${field('Category','category',p.category,80)}${field('Public location (optional)','location',p.location,100)}<div class="website-edit-images">${p.images.map((im,n)=>`<div><img src="${esc(asset(im.asset))}" alt="">${field('Image description','alt_'+n,im.alt,250)}<label><input type="checkbox" name="remove_${n}"> Remove from gallery</label></div>`).join('')}</div><label>Add gallery images<input type="file" name="images" multiple accept="image/jpeg,image/png,image/webp"></label><p class="form-hint">The first image is the project cover. New photographs are added at the end.</p>${footer('Save public copy')}</form>`,true);}
 function testimonialModal(id){const t=data.draft.testimonials.find(t=>t.id===id)||{id:uid(),name:'',title:'',content:'',project:'',photo:'',video:'',placement:'both',approved:false};portrait=t.photo;openModal('Client testimonial',`<form data-form="website-testimonial" data-id="${t.id}">${field('Name','name',t.name,120)}${field('Title / role','title',t.title,120)}${field('Quote','content',t.content,2000,true)}<label>Project<select name="project"><option value="">Studio testimonial</option>${data.draft.projects.map(p=>`<option value="${p.id}" ${p.id===t.project?'selected':''}>${esc(p.title)}</option>`).join('')}</select></label><label>Placement<select name="placement">${[['both','Across the page'],['home','Testimonials section'],['project','With this project']].map(([id,label])=>`<option value="${id}" ${id===t.placement?'selected':''}>${label}</option>`).join('')}</select></label>${field('YouTube video link (optional)','video',t.video,2048)}<label>Photo (optional)<input name="photo" type="file" accept="image/jpeg,image/png,image/webp"></label>${portrait?'<label><input type="checkbox" name="remove_photo"> Remove current photo</label>':''}<label class="check-label"><input name="approved" type="checkbox" ${t.approved?'checked':''}> I have permission to publish this testimonial and its media.</label>${footer('Save testimonial')}</form>`);}
 async function upload(file){if(file.size>20*1024*1024)throw Error('Choose an image smaller than 20 MB.');const encoded=await new Promise((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(r.result.split(',')[1]);r.onerror=reject;r.readAsDataURL(file);});return (await api('website_upload',{data:encoded})).id;}
 async function action(a,el){
  if(busy)return;busy=true;freeze(true);el.disabled=true;
  try{
   const act=a.replace(/^website-?/,'');
   if(!act||act==='open-project'){if(data&&studio===state.studio.id)await save();closeModal();state.data=null;state.present=false;state.tab='website';await load();render();if(act==='open-project')await importModal(el.dataset.id);return;}
   if(!data||studio!==state.studio.id)await load();
   if(act==='reset'){openModal('Remove website and start from scratch?',`<p>This permanently removes your current website design, chat, website images, project copies, testimonials, and all saved versions. Your published website will be taken offline.</p><p>Your original StudioDeck projects stay safe. Your domain connection and Website subscription are kept; this does not cancel billing.</p><p>You’ll return to the welcome screen to choose a new starting point. This cannot be undone.</p><div class="modal-footer">${button('Keep my website','close-modal','ghost')}${btn('Remove website & start over','confirm-reset','danger-text')}</div>`);}
   if(act==='confirm-reset'){const result=await api('website_reset',{revision:data.revision,confirm:true});closeModal();galleryOpen=false;previewTemplate='';renderGallery();tab='chat';sourceFile='index.html';message='';source=null;portrait='';pendingPrompt='';accept(result);toast('Website removed. Choose a new starting point.');}
   if(act==='save'){await save();toast('Website draft saved.');}
   if(act==='tab'){tab=el.dataset.tab;render();}
   if(act==='gallery'){await save();galleryOpen=true;previewTemplate='';renderGallery();}
   if(act==='gallery-close'){galleryOpen=false;renderGallery();}
   if(act==='gallery-back'){previewTemplate='';renderGallery();}
   if(act==='example'){previewTemplate=el.dataset.template;renderGallery();}
   if(act==='choose-template')await chooseTemplate(el.dataset.template);
   if(act==='start'){closeModal();accept(await api('website_start',{template:el.dataset.template,revision:data.revision}));}
   if(act==='blank'){const next=copy(data.draft);next.started=true;next.files={'index.html':'<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>'+esc(next.name)+'</title><link rel="stylesheet" href="styles.css"><script src="script.js" defer></script></head><body><main><h1>'+esc(next.name)+'</h1></main></body></html>','styles.css':'body { margin: 0; padding: 8vw; font-family: system-ui; }','script.js':''};accept(await api('website_save',{draft:next,revision:data.revision}));}
   if(act==='file'){sourceFile=el.dataset.file;render();}
   if(act==='copy-source'){await navigator.clipboard.writeText(data.draft.files[sourceFile]);toast('Source copied.');}
   if(act==='prompt'){const input=document.getElementById('website-prompt');input.value=el.dataset.prompt;setTimeout(()=>input.focus(),0);}
   if(act==='device'){document.querySelector('.website-preview').classList.toggle('mobile',el.dataset.size==='mobile');}
   if(act==='section'){const i=Number(el.dataset.index),j=i+Number(el.dataset.direction);[data.draft.sections[i],data.draft.sections[j]]=[data.draft.sections[j],data.draft.sections[i]];dirty=true;await save();}
   if(act==='remove-logo'){data.draft.logo='';dirty=true;await save();}
   if(act==='choose-project'){await save();openModal('Choose a project',`<p>Select a project to prepare its public website copy.</p>${data.projects.map(p=>btn(esc(p.name)+(p.archived?' · Archived':''),'import','',`data-id="${p.id}"`)).join(' ')||'<p>Create a project in StudioDeck first.</p>'}`);}
   if(act==='import')await importModal(el.dataset.id);
   if(act==='edit-project'){await save();editProject(el.dataset.id);}
   if(act==='toggle-project'){data.draft.projects.find(p=>p.id===el.dataset.id).included=!data.draft.projects.find(p=>p.id===el.dataset.id).included;dirty=true;await save();}
   if(act==='testimonial'){await save();testimonialModal(el.dataset.id);}
   if(act==='remove-testimonial'){data.draft.testimonials=data.draft.testimonials.filter(t=>t.id!==el.dataset.id);dirty=true;await save();}
   if(act==='undo'){accept(await api('website_undo',{revision:data.revision}));}
   if(act==='publish'){await save();if(!data.billing.active){tab='publish';render();toast('Activate Website to publish. Your draft is saved.');return;}const checks=data.checks||{errors:[],warnings:[]};if(checks.errors.length){tab='source';render();toast(checks.errors.join(' '));return;}toast('Checking the website at desktop and mobile sizes…');const results=await Promise.all([checkViewport(390),checkViewport(1280)]);const problems=[...new Set(results.flatMap(r=>[...(r.errors||[]),...(r.missingImages?['Some images did not load.']:[])]))],warnings=[...checks.warnings,...results.filter(r=>r.overflow).map(r=>`The page overflows at ${r.width}px. Review this layout before publishing.`)];openModal('Publish your website',`<p>Your saved one-page website will become public. The current version stays live until the new files are ready.</p>${problems.map(p=>`<p class="form-error">${esc(p)}</p>`).join('')}${warnings.map(p=>`<p class="form-hint">${esc(p)}</p>`).join('')}<div class="modal-footer">${button('Keep editing','close-modal','ghost')}${btn('Publish now','confirm-publish','primary',problems.length?'disabled':'')}</div>`);}
   if(act==='confirm-publish'){message='Optimizing images and preparing your website…';const result=await api('website_publish',{revision:data.revision});closeModal();message='Your website is published.';accept(result);toast(message);}
   if(act==='restore'){await save();accept(await api('website_restore',{release:el.dataset.id,revision:data.revision}));toast('Earlier version restored to draft. Preview before publishing.');}
   if(act==='checkout'){await save();const result=await api('website_checkout');location.assign(result.url);}
   if(act==='refresh-billing'){accept(await api('website_refresh_billing'));toast(data.billing.active?'Website subscription active.':'Payment is not confirmed yet.');}
  }finally{busy=false;freeze(false);el.disabled=false;}
 }
 async function submit(type,form){
  if(busy)return;busy=true;freeze(true);const submit=form.querySelector('[type=submit]');if(submit)submit.disabled=true;
  try{const values=Object.fromEntries(new FormData(form));
   if(type==='website-details'){await save();toast('Draft saved. Preview updated.');}
   if(type==='website-chat'){await save();message='Working on your draft…';toast(message);const result=await api('website_chat',{prompt:values.prompt,revision:data.revision});message=result.message;accept(result.website);if(result.project_id)await importModal(result.project_id,result.prompt);}
   if(type==='website-import'){const ids=new FormData(form).getAll('image');if(ids.length>20)throw Error('Select up to 20 images.');const result=await api('website_import',{project_id:source.project.id,revision:data.revision,title:values.title,description:values.description,images:ids.map(id=>({id,alt:values['alt_'+id]}))});closeModal();tab='projects';accept(result);if(pendingPrompt&&data.ai){const prompt=pendingPrompt;pendingPrompt='';try{const changed=await api('website_chat',{prompt:prompt+' Use the newly reviewed public project copy already in the library; integrate it into the current page.',revision:data.revision});message=changed.message;tab='chat';accept(changed.website);}catch(e){message='The public project copy is saved. '+e.message;toast(message);render();}}}
   if(type==='website-project'){const next=copy(data.draft),p=next.projects.find(p=>p.id===form.dataset.id);for(const key of ['title','description','category','location'])p[key]=values[key];p.images=p.images.map((im,n)=>({...im,alt:values['alt_'+n],remove:values['remove_'+n]==='on'})).filter(im=>!im.remove);for(const file of form.elements.images.files){const asset=await upload(file);p.images.push({asset,alt:file.name.replace(/\.[^.]+$/,'').replace(/[-_]/g,' ')});}const result=await api('website_save',{draft:next,revision:data.revision});closeModal();accept(result);}
   if(type==='website-testimonial'){const file=form.elements.photo.files[0];const t={id:form.dataset.id,name:values.name,title:values.title,content:values.content,project:values.project,video:values.video,placement:values.placement,approved:values.approved==='on',photo:file?await upload(file):values.remove_photo==='on'?'':portrait};const next=copy(data.draft),index=next.testimonials.findIndex(x=>x.id===t.id);if(index<0)next.testimonials.push(t);else next.testimonials[index]=t;const result=await api('website_save',{draft:next,revision:data.revision});closeModal();accept(result);}
   if(type==='website-domain'){accept(await api('website_domain',{name:values.name}));}
  }finally{busy=false;freeze(false);if(submit)submit.disabled=false;}
 }
 document.addEventListener('input',e=>{const file=e.target.dataset.websiteFile;if(!data||!file)return;data.draft.files[file]=e.target.value;dirty=true;document.querySelector('.website-status span').textContent='Unsaved changes · Save to refresh preview';const b=document.querySelector('[data-action="website-save"]');if(b)b.disabled=false;});
 document.addEventListener('change',async e=>{if(!e.target.hasAttribute('data-website-files')||busy)return;try{for(const f of e.target.files){if(!Object.hasOwn(data.draft.files,f.name))throw Error('Choose index.html, styles.css or script.js.');if(f.size>160000)throw Error('This source file is too large.');data.draft.files[f.name]=await f.text();}dirty=true;render();toast('Files loaded into the editor. Save to update the preview.');}catch(error){toast(error.message);}});
 window.addEventListener('message',e=>{const frame=document.querySelector('.website-preview iframe');if(!frame||e.source!==frame.contentWindow||e.data?.type!=='website-preview-report'||e.data.channel!==previewChannel)return;previewReport=e.data;const status=document.querySelector('.website-preview-health');if(status)status.textContent=e.data.errors?.length?'Preview issue: '+String(e.data.errors[0]).slice(0,250):e.data.missingImages?'Some images did not load.':e.data.overflow?'This layout is wider than the preview.':'Preview loaded · Interactions are ready to try';});
 document.addEventListener('keydown',e=>{const gallery=document.getElementById('website-gallery');if(!gallery)return;if(e.key==='Escape'){e.preventDefault();galleryOpen=false;renderGallery();}if(e.key==='Tab'){const nodes=[...gallery.querySelectorAll('button:not([disabled]),iframe.website-full-example')];const first=nodes[0],last=nodes.at(-1);if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus();}}});
 window.addEventListener('beforeunload',e=>{if(dirty){e.preventDefault();e.returnValue='';}});
 return {page,load,action,submit};
}
