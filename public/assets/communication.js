import {composerFields,initComposers,selectComposerType,composerText,threadTypes,plainMessageFields,normalizeThreadType} from './communication-composer.js';
import {mentionData,mentionBody} from './mentions.js';
import {getLanguage,tr} from './i18n.js';

export function communicationUi({state,api,render,refresh,openModal,closeModal,toast,button,icon,esc,personAvatar,slideDefs,startPresentation,markCommentsRead,hydrateCommentPreviews,openProject,suggestions}) {
  const nl={'Waiting for reply':'Wacht op reactie','Needs resolution':'Moet worden opgelost','Resolved':'Opgelost','Mark resolved':'Markeer als opgelost','Invite to this conversation':'Uitnodigen voor dit gesprek','Sending this request invites the selected person to this conversation and its attachments, including earlier messages. They will not receive presentation or project budget access.':'Met dit verzoek nodig je de geselecteerde persoon uit voor dit gesprek en de bijlagen, inclusief eerdere berichten. Zij krijgen geen toegang tot de presentatie of projectbegroting.','Conversation guests':'Gasten in dit gesprek','Remove access':'Toegang intrekken','Remove conversation access?':'Toegang tot dit gesprek intrekken?','This person will no longer be able to read or reply to this conversation or download its attachments.':'Deze persoon kan dit gesprek niet meer lezen of beantwoorden en de bijlagen niet meer downloaden.','Access removed.':'Toegang ingetrokken.','New conversation':'Nieuw gesprek','Team':'Team','Clients':'Opdrachtgevers','Other people involved':'Overige betrokkenen','Needs access':'Toegang nodig','Email address missing':'E-mailadres ontbreekt','Add an email address in People to invite a contact. Only the project team can invite new people to a conversation.':'Voeg een e-mailadres toe bij Personen om iemand uit te nodigen. Alleen het projectteam kan nieuwe personen uitnodigen voor een gesprek.','Subject':'Onderwerp','Start conversation':'Gesprek starten','Conversation started.':'Gesprek gestart.',Communication:'Communicatie','All communication':'Alle communicatie',Confirmations:'Bevestigingen','Pending only':'Alleen openstaand',Everyone:'Iedereen','Needs my confirmation':'Wacht op mijn bevestiging','Waiting for someone else':'Wacht op iemand anders','Post reply':'Reactie plaatsen','Ask for confirmation':'Om bevestiging vragen','Confirm with':'Bevestiging vragen aan',Message:'Bericht','Include a budget change':'Budgetwijziging toevoegen','Budget change (€) · including VAT':'Budgetwijziging (€) · inclusief btw','including VAT':'inclusief btw','Added to the budget only after confirmation.':'Wordt pas na bevestiging aan het budget toegevoegd.','Use a positive amount for extra cost (1000), or a negative amount for a cheaper option (−250). Enter the change, not the new total.':'Gebruik een positief bedrag voor extra kosten (1000), of een negatief bedrag voor een goedkopere optie (−250). Vul de wijziging in, niet het nieuwe totaal.','Pending confirmation':'Wacht op bevestiging',Confirmed:'Bevestigd',Withdrawn:'Ingetrokken',Confirm:'Bevestigen',Withdraw:'Intrekken',Reply:'Reageren',Cancel:'Annuleren','Send confirmation request':'Verstuur bevestigingsverzoek','General conversation':'Algemeen gesprek',Conversations:'Gesprekken','Slide comments included':'Inclusief opmerkingen bij dia’s','Discuss the details. Ask for a go-ahead. Keep the answer here.':'Bespreek de details. Vraag om akkoord. Bewaar het antwoord hier.','Shared with the project team and clients with access to this iteration.':'Gedeeld met het projectteam en klanten met toegang tot deze iteratie.','No messages yet. Start the conversation below.':'Nog geen berichten. Begin hieronder een gesprek.','No confirmations match this view.':'Geen bevestigingen voor dit filter.','Open conversation':'Gesprek openen','Open original slide':'Oorspronkelijke dia openen','Attach a file':'Bestand toevoegen',optional:'optioneel','Choose a project file':'Kies een projectbestand','No file attached':'Geen bestand toegevoegd','Or upload and link a file':'Of upload en koppel een bestand','Linked · processing in the background':'Gekoppeld · wordt op de achtergrond verwerkt','Uploading…':'Uploaden…','View confirmation':'Bevestiging bekijken','Added to budget':'Toegevoegd aan budget','Budget change':'Budgetwijziging','Back to presentation':'Terug naar presentatie','Withdraw this request?':'Dit verzoek intrekken?','It will remain in the conversation as withdrawn. The budget will not change.':'Het blijft als ingetrokken in het gesprek staan. Het budget verandert niet.','Request sent.':'Verzoek verstuurd.','Reply posted.':'Reactie geplaatst.','Confirmation recorded.':'Bevestiging vastgelegd.','Request withdrawn.':'Verzoek ingetrokken.','Waiting for':'Wacht op','Confirmed by':'Bevestigd door','Requested by':'Aangevraagd door','No other participants have access to this iteration yet. Add a team member or share this iteration with a client first.':'Er hebben nog geen andere deelnemers toegang tot deze iteratie. Voeg een teamlid toe of deel deze iteratie eerst met een klant.','Adding a file is unavailable while this iteration is locked.':'Een bestand toevoegen kan niet zolang deze iteratie vergrendeld is.','Replying to a confirmation request':'Reactie op een bevestigingsverzoek','This exact file version stays attached to the message.':'Deze exacte bestandsversie blijft aan het bericht gekoppeld.','Download file':'Bestand downloaden',pending:'openstaand','Read-only project access':'Alleen leestoegang tot dit project','View pending confirmations':'Openstaande bevestigingen bekijken','Supported files: PDF, PowerPoint, Excel, CSV, JPG, PNG and WebP.':'Ondersteunde bestanden: PDF, PowerPoint, Excel, CSV, JPG, PNG en WebP.','Maximum file size: 100 MB.':'Maximale bestandsgrootte: 100 MB.','Linked documents are processed without adding costs to the budget.':'Gekoppelde documenten worden verwerkt zonder kosten aan het budget toe te voegen.'};
  Object.assign(nl,{'Thread details':'Gespreksgegevens','Change thread':'Gesprek wijzigen','New linked thread':'Nieuw gekoppeld gesprek','Linked conversations':'Gekoppelde gesprekken','Linked to':'Gekoppeld aan','Thread history':'Gespreksgeschiedenis','Open':'Open','Answered':'Beantwoord','Mark complete':'Markeer als afgerond','Reopen':'Heropenen','This starts a separate conversation. Its approval and completion are tracked independently.':'Dit start een apart gesprek. Akkoord en afronding worden afzonderlijk bijgehouden.','Needs attention':'Aandacht nodig','Unread messages, open questions or to dos, and pending approvals.':'Ongelezen berichten, open vragen of taken en openstaande bevestigingen.','View evidence':'Bron bekijken','Studio only':'Alleen studio','Shared':'Gedeeld','Audience':'Zichtbaar voor','Team and clients with access to this iteration':'Team en klanten met toegang tot deze iteratie','Shared with clients who can access the original iteration.':'Gedeeld met klanten die toegang hebben tot de oorspronkelijke iteratie.','Every subject, from first question to completed work.':'Elk onderwerp, van eerste vraag tot afgerond werk.','Communication views':'Communicatieweergaven','Unread':'Ongelezen','unread':'ongelezen','Needs my response':'Wacht op mijn reactie','Open actions':'Open acties','Open action':'Open actie','open actions':'open acties','Awaiting approval':'Wacht op akkoord','awaiting approval':'wacht op akkoord','Completed':'Afgerond','Add action':'Actie toevoegen','Share conversation':'Gesprek delen','Reopen discussion':'Gesprek heropenen','Mark answered':'Markeren als beantwoord','No conversations match this view.':'Geen gesprekken voor dit filter.','Keep the whole subject together.':'Houd het hele onderwerp bij elkaar.','Start a conversation to discuss a question, track actions, or request approval.':'Start een gesprek om een vraag te bespreken, acties bij te houden of akkoord te vragen.','A conversation keeps its original audience and source iteration as the project progresses.':'Een gesprek behoudt zijn oorspronkelijke deelnemers en broniteratie terwijl het project verdergaat.','Share this conversation first to request approval from a client.':'Deel dit gesprek eerst om akkoord van een klant te vragen.','Clients with access to the original iteration will be able to read all earlier messages and attachments. Studio-only action details stay private until you share those items.':'Klanten met toegang tot de oorspronkelijke iteratie kunnen alle eerdere berichten en bijlagen lezen. Actiedetails voor de studio blijven privé totdat je die items deelt.'});
  const t=s=>getLanguage()==='nl'?(nl[s]||s):s;
  let thread='',view='all',scope='',replyTo=null,highlight=null,busy=false;
  const drafts=new Map();
  const $=s=>document.querySelector(s);
  const data=()=>state.data?.communication;
  const enabled=()=>!!data();
  const actor=()=>data()?.actor;
  const canWrite=()=>state.client||state.data?.can_edit!==false;
  const btn=(text,action,kind='',extra='',ico='')=>button(t(text),'comm-'+action,kind,extra,ico);
  const time=s=>new Date(s).toLocaleString(getLanguage()==='nl'?'nl-NL':'en-GB',{day:'numeric',month:'short',hour:'2-digit',minute:'2-digit'});
  const signed=n=>(n>0?'+ ':n<0?'− ':'')+new Intl.NumberFormat(getLanguage()==='nl'?'nl-NL':'en-IE',{style:'currency',currency:'EUR',maximumFractionDigits:n%100?2:0}).format(Math.abs(n)/100);
  const comments=()=>data()?.comments||state.data?.comments||[];
  const items=()=>data()?.items||state.data?.open_questions||[];
  const root=()=>comments().find(c=>c.id===thread);
  const topic=c=>c?.thread_details;
  const typeLabel=type=>composerText(threadTypes.find(([key])=>key===normalizeThreadType(type))?.[1]||'Conversation');
  const contextIteration=()=>root()?.iteration_id||state.data.iteration.id;
  const contextInfo=()=>data()?.iterations?.find(i=>i.id===contextIteration())||state.data.iteration;
  const recipients=()=>data()?.iteration_recipients?.[contextIteration()]||data()?.recipients||[];
  const privateThread=()=>root()?.audience==='studio';
  const threadItems=id=>items().filter(q=>q.thread_id===id&&!Number(q.dismissed));
  const openItems=id=>threadItems(id).filter(q=>!Number(q.resolved)&&(Number(q.accepted)||Number(q.published)));
  const threadRequests=id=>requests().filter(r=>threadFor({id:r.comment_id,parent_id:r.parent_id})===id&&r.status==='pending');
  const completed=c=>(!!Number(c.answered)||threadItems(c.id).length>0)&&!openItems(c.id).length&&!threadRequests(c.id).length;
  const audienceLabel=()=>t(privateThread()?'Studio only':'Shared with clients who can access the original iteration.');
  const requests=()=>data()?.confirmations||[];
  const pendingCount=()=>requests().filter(r=>r.status==='pending').length;
  const rootFor=c=>comments().find(x=>x.id===c.parent_id)||c;
  const threadTitle=id=>data()?.threads?.find(t=>t.id===id)?.title;
  const threadFor=c=>rootFor(c).id;
  const inThread=c=>threadFor(c)===thread;
  const name=email=>recipients().find(p=>p.email===email)?.name||comments().find(c=>c.author===email)?.profile?.name||email;
  function ensureScope(){const key=state.data?.project.id;if(scope!==key){scope=key;thread='';replyTo=null;highlight=null;}}
  function attachment(c){const a=data()?.attachments.find(a=>a.comment_id===c.id);return a?`<button class="comm-attachment" data-action="comm-download" data-id="${esc(a.id)}" data-iteration="${esc(c.iteration_id)}">${icon('file')}<span>${esc(a.name)} <small>· V${a.number}</small></span>${icon('download')}</button>`:'';}
  function card(r,compact=false){const checklist=items().find(q=>q.confirmation_id===r.comment_id&&!Number(q.dismissed));const c=comments().find(c=>c.id===r.comment_id)||{id:r.comment_id},done=r.status==='confirmed',withdrawn=r.status==='withdrawn';return `<article class="comm-confirmation is-${r.status}${highlight===r.comment_id?' is-highlighted':''}" data-confirmation="${esc(r.comment_id)}"><div class="comm-confirmation-top"><span class="comm-status ${done?'done':withdrawn?'closed':'pending'}">${icon(done?'check':withdrawn?'close':'clock')}${t(done?'Confirmed':withdrawn?'Withdrawn':'Pending confirmation')}</span><small>${esc(name(r.author))} → ${esc(r.recipient_name)} · ${time(r.created_at)}</small></div>${!topic(c)?`<p>${mentionBody(r.body,comments().find(c=>c.id===r.comment_id)?.mentions)}</p>${attachment(c)}`:''}${r.amount_cents!==null?`<div class="comm-budget-change">${icon('budget')}<span>${t('Budget change')} <strong>${signed(r.amount_cents)}</strong> <small>${t('including VAT')}</small></span></div>`:''}<div class="comm-confirmation-bottom">${done?`<span class="comm-confirmed-by">${icon('check')}${t('Confirmed by')} ${esc(r.recipient_name)} · ${time(r.decided_at)}</span>${r.amount_cents!==null?btn('Added to budget','budget','small ghost',`data-id="${esc(r.comment_id)}"`):''}`:withdrawn?`<small>${t('Withdrawn')} · ${time(r.decided_at)}</small>`:`${canWrite()&&r.recipient===actor()?btn(r.amount_cents!==null?t('Confirm')+' · '+signed(r.amount_cents):'Confirm','confirm','primary small',`data-id="${esc(r.comment_id)}" ${r.amount_cents!==null&&Number(data().iterations?.find(i=>i.id===r.iteration_id)?.locked)?'disabled':''}`,'check'):`<small class="muted">${t('Waiting for')} ${esc(r.recipient_name)}</small>`}${canWrite()?btn('Reply','reply','small ghost',`data-id="${esc(r.comment_id)}"`):''}${canWrite()&&r.author===actor()?btn('Withdraw','withdraw','small ghost',`data-id="${esc(r.comment_id)}"`):''}`}${checklist?button(tr('checklist_view_item'),'discuss-open-question','small ghost',`data-id="${esc(checklist.id)}"`):''}${compact?btn('Open conversation','open','small ghost',`data-id="${esc(r.comment_id)}"`,'arrow'):''}</div></article>`;}
  function message(c){const r=requests().find(r=>r.comment_id===c.id);return r&&!topic(c)?card(r):`<article class="comm-message" data-message="${esc(c.id)}"><div class="comment-author">${personAvatar(c.profile,c.author)}<small><strong>${esc(c.profile?.name||name(c.author))}</strong> · ${time(c.created_at)}</small></div><div class="comm-message-body"><p>${mentionBody(c.body,c.mentions)}</p>${attachment(c)}</div></article>`;}
  function threadDetails(c){const d=topic(c)||{type:'conversation'},request=requests().find(r=>r.comment_id===c.id),done=d.question_id?!!d.resolved:!!Number(c.answered),canManage=canWrite()&&(!state.client||c.author===actor()),canComplete=canWrite()&&!Number(contextInfo().locked)&&(!state.client||c.author===actor()||d.assignee===actor());
    const evidence=items().find(q=>q.id===d.question_id)?.citations||[];
    const linked=comments().filter(other=>!other.parent_id&&(other.thread_details?.related_root_id===c.id||other.id===d.related_root_id));
    const status=request?(request.status==='confirmed'?'Confirmed':request.status==='withdrawn'?'Withdrawn':'Pending confirmation'):done?(d.type==='conversation'?'Resolved':'Completed'):'Open';
    const statusClass=request?(request.status==='confirmed'?'done':request.status==='withdrawn'?'closed':'pending'):done?'done':'pending';
    const requestMeta=request?`<p><span>${t(request.status==='confirmed'?'Confirmed by':request.status==='withdrawn'?'Requested by':'Waiting for')}</span><strong>${esc(request.status==='withdrawn'?name(request.author):request.recipient_name)}</strong>${request.decided_at?`<small>${time(request.decided_at)}</small>`:''}</p>${request.amount_cents!==null?`<p class="comm-summary-amount"><span>${t('Budget change')}</span><strong>${signed(request.amount_cents)}</strong><small>${t('including VAT')}</small></p>`:''}`:'';
    const requestActions=request?(request.status==='pending'?`${canWrite()&&request.recipient===actor()?btn(request.amount_cents!==null?t('Confirm')+' · '+signed(request.amount_cents):'Confirm','confirm','primary small',`data-id="${esc(request.comment_id)}" ${request.amount_cents!==null&&Number(contextInfo().locked)?'disabled':''}`,'check'):''}${canWrite()&&request.author===actor()?btn('Withdraw','withdraw','small ghost',`data-id="${esc(request.comment_id)}"`):''}`:request.status==='confirmed'&&request.amount_cents!==null?btn('Added to budget','budget','small ghost',`data-id="${esc(request.comment_id)}"`):''):'';
    return `<div class="comm-thread-details" data-thread-type="${esc(d.type)}"><article class="comm-thread-summary${request?' comm-confirmation is-'+request.status:''}${highlight===c.id?' is-highlighted':''}" ${request?`data-confirmation="${esc(c.id)}"`:''}><div class="comm-summary-top"><span class="comm-type-label">${typeLabel(d.type)}</span><span class="comm-status ${statusClass}">${t(status)}</span></div><div class="comm-summary-title"><h2>${esc(threadTitle(c.id)||c.body.slice(0,90))}</h2><p class="muted">${audienceLabel()} · V${c.iteration_number||1}</p></div><div class="comm-summary-meta">${d.assignee_name?`<p><span>${composerText(d.type==='conversation'?'Waiting for':'Responsible')}</span><strong>${esc(d.assignee_name)}</strong></p>`:''}${d.due_date?`<p><span>${composerText('Due date (optional)').replace(' (optional)','').replace(' (optioneel)','')}</span><strong>${esc(d.due_date)}</strong></p>`:''}${requestMeta}</div><div class="comm-summary-actions">${d.question_id&&canComplete?btn(done?'Reopen':d.type==='conversation'?'Mark resolved':'Mark complete','work','primary small',`data-id="${esc(c.id)}" data-resolved="${!done}"`):''}${d.type==='conversation'&&!d.question_id&&canWrite()?btn(done?'Reopen':'Mark resolved','answered','primary small',`data-id="${esc(c.id)}"`):''}${requestActions}${!state.client&&canWrite()&&privateThread()?btn('Share conversation','share','small ghost'):''}${canManage&&!Number(contextInfo().locked)&&['conversation','todo'].includes(d.type)?btn('Change thread','edit-thread','small ghost'):''}${canWrite()?btn('New linked thread','linked','small ghost','','plus'):''}</div></article>${linked.length?`<div class="comm-linked"><small>${t('Linked conversations')}</small>${linked.map(other=>btn(threadTitle(other.id)||other.body.slice(0,90),'open','small ghost',`data-id="${esc(other.id)}"`)).join('')}</div>`:''}${evidence.length?`<details class="comm-thread-history"><summary>${t('View evidence')}</summary>${evidence.map(e=>`<blockquote>${esc(e.quote)}<small>${esc(e.name)}${e.page?' · p. '+e.page:''}</small>${btn('View evidence','evidence','small ghost',`data-id="${esc(e.version_id)}" data-page="${Number(e.page)||0}" data-iteration="${esc(c.iteration_id)}"`)}</blockquote>`).join('')}</details>`:''}${d.history?.length?`<details class="comm-thread-history"><summary>${t('Thread history')}</summary>${d.history.map(h=>`<p><small>${esc(name(h.actor))} · ${time(h.created_at)}</small><br>${typeLabel(h.from_type)} → ${typeLabel(h.to_type)}${h.assignee_name?' · '+esc(h.assignee_name):''}${h.due_date?' · '+esc(h.due_date):''}</p>`).join('')}</details>`:''}</div>`;
  }
  function editThread(){const c=root(),d=topic(c)||{type:'conversation'};openModal(t('Change thread'),`<form id="comm-edit-thread-form" data-edit-thread="1">${composerFields({people:recipients().filter(p=>!privateThread()||p.group==='team'),actor:actor(),type:d.type,types:threadTypes.filter(([key])=>d.type==='todo'?key==='todo':['conversation','todo'].includes(key)),showBody:false})}<p class="form-error" data-comm-error role="alert" hidden></p><div class="modal-footer">${button(t('Cancel'),'close-modal','ghost')}<button type="submit" class="button primary">${composerText('Save thread')}</button></div></form>`);const form=$('#comm-edit-thread-form');form.elements.assignee.value=d.assignee||'';form.elements.due_date.value=d.due_date||'';initComposers(document.querySelector('.modal'));}
  function attachFields(fresh=false){return `<details class="comm-attach-details"><summary>${icon('file')}${t('Attach a file')} <span class="muted">${t('optional')}</span></summary><div class="comm-attach-fields"><label>${t('Choose a project file')}<select name="version_id"><option value="">${t('No file attached')}</option>${(fresh?state.data.files||[]:data()?.iteration_files?.[contextIteration()]||state.data.files||[]).map(f=>`<option value="${esc(f.id)}">${esc(f.name)} · V${f.number}</option>`).join('')}</select></label>${!Number(fresh?state.data.iteration.locked:contextInfo().locked)?`<label class="comm-upload-label">${icon('upload')}${t('Or upload and link a file')}<input type="file" data-comm-upload accept=".pdf,.png,.jpg,.jpeg,.webp,.ppt,.pptx,.xls,.xlsx,.csv"></label><small class="form-hint">${t('Supported files: PDF, PowerPoint, Excel, CSV, JPG, PNG and WebP.')} ${t('Maximum file size: 100 MB.')} ${t('Linked documents are processed without adding costs to the budget.')}</small>`:`<small>${t('Adding a file is unavailable while this iteration is locked.')}</small>`}<div data-comm-upload-status role="status"></div></div></details>`;}
  function itemCard(q){const source=comments().find(c=>c.thread_details?.question_id===q.id),details=source?.thread_details;const canComplete=source&&canWrite()&&!q.locked&&(!state.client||source.author===actor()||details.assignee===actor());const canEdit=!state.client&&canWrite()&&!q.locked;
    return `<article class="comm-work-item ${Number(q.resolved)?'is-done':''}"><div class="row between"><span class="tag outline">${tr(q.item_type==='action'?'checklist_action':'question')}</span><span class="tag">${tr(Number(q.published)?'checklist_shared':'checklist_private')}</span></div><h3>${esc(q.question)}</h3>${q.responsible?`<p>${tr('checklist_responsible')}: ${esc(q.responsible)}</p>`:''}${details?.due_date?`<p>${composerText('Due date (optional)').replace(' (optional)','').replace(' (optioneel)','')}: ${esc(details.due_date)}</p>`:''}${q.reason?`<p>${esc(q.reason)}</p>`:''}${q.answer?`<p class="question-answer">${esc(q.answer)}</p>`:''}${q.stale?`<p class="notice">${tr('checklist_stale')}</p>`:''}${(q.citations||[]).map(c=>`<blockquote>${esc(c.quote)}<small>${esc(c.name)}${c.page?' · p. '+c.page:''}</small>${btn('View evidence','evidence','small ghost',`data-id="${esc(c.version_id)}" data-page="${Number(c.page)||0}" data-iteration="${esc(q.iteration_id)}"`)}</blockquote>`).join('')}<div class="row wrap">${canComplete?btn(tr(Number(q.resolved)?'checklist_reopen':'checklist_done'),'work','small',`data-id="${esc(source.id)}" data-resolved="${!Number(q.resolved)}"`):''}${canEdit?(!canComplete?button(tr(Number(q.resolved)?'checklist_reopen':'checklist_done'),'change-open-question','small',`data-id="${esc(q.id)}" data-operation="${Number(q.resolved)?'reopen':'resolve'}"`):'')+button(tr('edit'),'edit-open-question','small ghost',`data-id="${esc(q.id)}"`):`<span>${t(Number(q.resolved)?'Completed':'Open action')}</span>`}${canEdit&&q.confirmation?.status!=='pending'?button(tr('checklist_request_approval'),'checklist-request-approval','small ghost',`data-id="${esc(q.id)}"`):''}</div></article>`;
  }
  function matches(c){const messages=comments().filter(m=>threadFor(m)===c.id),pending=threadRequests(c.id);
    if(view==='attention')return messages.some(m=>m.unread)||openItems(c.id).length>0||pending.length>0;
    if(view==='unread')return messages.some(m=>m.unread);
    if(view==='mine')return messages.some(m=>m.thread_details?.assignee===actor()&&openItems(c.id).some(q=>q.id===m.thread_details.question_id))||pending.some(r=>r.recipient===actor())||messages.some(m=>m.unread&&m.mentions?.some(p=>p.email===actor()));
    if(view==='actions')return openItems(c.id).length>0;
    if(view==='confirmations')return pending.length>0;
    if(view==='completed')return completed(c);
    return true;
  }
  function page(){ensureScope();if(!state.client&&!['all','attention'].includes(view))view='attention';const roots=comments().filter(c=>!c.parent_id).sort((a,b)=>{
      const latest=c=>comments().filter(m=>threadFor(m)===c.id).at(-1)?.created_at||c.created_at;
      return latest(b).localeCompare(latest(a));
    }),filtered=roots.filter(matches);
    if(!filtered.some(c=>c.id===thread))thread=filtered[0]?.id||'';
    const selected=root(),entries=comments().filter(inThread).reverse(),work=threadItems(thread).filter(q=>q.id!==topic(selected)?.question_id);
    const tabs=[['all','All communication'],['attention','Needs attention'],...(state.client?[['unread','Unread'],['mine','Needs my response'],['actions','Open actions'],['confirmations','Awaiting approval'],['completed','Completed']]:[])];
    const heading=`<div class="section-title comm-section-title"><div><h2>${t('Communication')}</h2><p class="muted">${t('Every subject, from first question to completed work.')}</p></div><div class="row wrap">${!state.client&&canWrite()?button(tr('checklist_suggest'),'check-run','small ghost','','spark'):''}${canWrite()?btn('New conversation','new','primary','','plus'):''}</div></div><div class="filter-chips comm-view-tabs" aria-label="${t('Communication views')}">${tabs.map(([key,label])=>btn(label,'view','small '+(view===key?'primary':''),`data-view="${key}" aria-pressed="${view===key}"`)).join('')}</div>`;
    const suggestionList=!state.client?(suggestions?.()||''):'';
    return heading+(view==='attention'?`<p class="muted">${t('Unread messages, open questions or to dos, and pending approvals.')}</p>`:'')+suggestionList+`<div class="comm-hub"><aside class="comm-topic-list">${filtered.map(c=>{const unread=comments().filter(m=>threadFor(m)===c.id&&m.unread).length,actions=openItems(c.id).length,pending=threadRequests(c.id).length;return `<button class="comm-topic ${thread===c.id?'selected':''}" data-action="comm-thread" data-thread-type="${esc(normalizeThreadType(topic(c)?.type))}" data-id="${esc(c.id)}" aria-pressed="${thread===c.id}"><strong>${esc(threadTitle(c.id)||c.body.slice(0,90))}</strong><span><i class="comm-type-dot" aria-hidden="true"></i>${typeLabel(topic(c)?.type)} · ${c.audience==='studio'?t('Studio only'):t('Shared')} · V${c.iteration_number||1}</span><span>${[unread?unread+' '+t('unread'):'',actions?(normalizeThreadType(topic(c)?.type)==='conversation'?t(topic(c)?.assignee?'Waiting for reply':'Needs resolution'):actions+' '+t('open actions')):'',pending?pending+' '+t('awaiting approval'):'',completed(c)?t('Completed'):''].filter(Boolean).join(' · ')}</span></button>`;}).join('')||`<p class="comm-empty">${t('No conversations match this view.')}</p>`}</aside><section class="comm-thread">${selected?`<header class="comm-thread-head">${threadDetails(selected)}</header>${canWrite()?`<form id="comm-reply-form" class="comm-composer">${replyTo?`<div class="comm-replying">${t('Replying to a confirmation request')}${btn('Cancel','cancel-reply','small ghost')}</div>`:''}${plainMessageFields({body:drafts.get(scope+thread)||'',id:'comm-reply',attachments:attachFields()})}<div class="comm-compose-actions"><small>${audienceLabel()}</small><button class="button primary" type="submit">${composerText('Send message')}</button></div><p class="form-error" data-comm-error role="alert" hidden></p></form>`:`<p class="notice">${t('Read-only project access')}</p>`}${guestList()}${selected.slide!=='general'?`<button class="comm-context" data-action="comm-source" data-slide="${esc(selected.slide)}" data-iteration="${esc(selected.iteration_id)}"><img data-comment-preview="${esc(selected.id)}" data-iteration="${esc(selected.iteration_id)}" alt="${t('Open original slide')}"><span>${t('Open original slide')}${selected.annotation?`<small>${tr(selected.annotation.image_version_id?'pin_on_variation':'pin_on_original')}</small>`:''}</span>${icon('arrow')}</button>`:''}${work.length?`<div class="comm-work-list">${work.map(itemCard).join('')}</div>`:''}<div class="comm-messages">${entries.map(message).join('')}</div>`:`<div class="comm-empty"><h2>${t('Keep the whole subject together.')}</h2><p>${t('Start a conversation to discuss a question, track actions, or request approval.')}</p></div>`}</section></div>`;
  }
  function guestList(){if(state.client||!canWrite())return '';const guests=(data().guests||[]).filter(g=>g.root_id===thread&&!Number(g.revoked)&&g.expires_at>Date.now()/1000);return guests.length?`<div class="comm-guests">${t('Conversation guests')}${guests.map(g=>`<div class="row between"><span>${esc(g.name)}</span>${btn('Remove access','revoke','small ghost',`data-id="${esc(g.id)}"`)}</div>`).join('')}</div>`:'';}
  function newConversation(slide='',annotation=null,related='',type='conversation',text='',checklist=null){
    replyTo=null;const pinImage=annotation?document.querySelector('.annotation-host img')?.src:'';const fresh=!related,iid=fresh?state.data.iteration.id:contextIteration(),people=(fresh?data()?.recipients||[]:recipients()).filter(p=>fresh||!privateThread()||p.group==='team');
    openModal(t(related?'New linked thread':'New conversation'),`<form id="comm-thread-form" data-iteration="${esc(iid)}">${related?`<input type="hidden" name="related_thread_id" value="${esc(related)}"><input type="hidden" name="audience" value="${privateThread()?'studio':'shared'}"><p class="notice">${t('Linked to')}: ${esc(threadTitle(related)||root()?.body.slice(0,90))}. ${t('This starts a separate conversation. Its approval and completion are tracked independently.')}</p>`:''}${checklist?`<input type="hidden" name="checklist_id" value="${esc(checklist.id)}">`:''}${pinImage?`<div class="comm-pin-preview"><img src="${esc(pinImage)}" alt="${esc(t('Open original slide'))}"><span style="left:${Number(annotation.x)*100}%;top:${Number(annotation.y)*100}%"></span></div>`:''}${slide?`<input type="hidden" name="slide" value="${esc(slide)}"><input type="hidden" name="annotation" value="${esc(JSON.stringify(annotation))}"><p class="form-hint">${t('Open original slide')}${annotation?' · '+tr(annotation.image_version_id?'pin_on_variation':'pin_on_original'):''}</p>`:''}${composerFields({people,actor:actor(),locked:!!Number(fresh?state.data.iteration.locked:contextInfo().locked),type,body:text,details:`<label>${t('Subject')}<input name="thread_title" maxlength="160" required></label>${!related&&!state.client&&!slide?`<label>${t('Audience')}<select name="audience"><option value="studio">${t('Studio only')}</option><option value="shared">${t('Team and clients with access to this iteration')}</option></select></label>`:''}`,attachments:attachFields(fresh)})}<p class="form-hint">${t('A conversation keeps its original audience and source iteration as the project progresses.')}</p><p class="form-error" data-comm-error role="alert" hidden></p><div class="modal-footer">${button(t('Cancel'),'close-modal','ghost')}<button type="submit" class="button primary">${t('Start conversation')}</button></div></form>`);initComposers(document.querySelector('.modal'));
  }
  function popup(text='',checklist=null){newConversation('',null,thread,'approval',text,checklist);}
  function show(){closeModal();if(state.client){state.communicationOpen=true;render();}else{state.present=false;state.tab='comments';render();}}
  function open(id){const c=comments().find(c=>c.id===id);if(!c)return;ensureScope();thread=threadFor(c);view='all';highlight=id;show();}
  function budgetSource(item){const r=item.confirmation;if(!r)return false;openModal(t('View confirmation'),`<p>${mentionBody(r.body,comments().find(c=>c.id===r.comment_id)?.mentions)}</p><div class="comm-budget-change"><strong>${signed(r.amount_cents)}</strong> ${t('including VAT')}</div><p>${t('Requested by')} ${esc(name(r.author))}</p><p>${t('Confirmed by')} ${esc(r.recipient_name)} · ${time(r.decided_at)}</p>${r.iteration_id===state.data.iteration.id?btn('Open conversation','open','primary',`data-id="${esc(r.comment_id)}"`):!state.client?btn('Open conversation','location','primary',`data-id="${esc(r.comment_id)}" data-project="${esc(state.data.project.id)}" data-iteration="${esc(r.iteration_id)}"`):''}`);return true;}
  function afterRender(){if(!enabled())return;initComposers();hydrateCommentPreviews?.();const nav=document.querySelector('.tabs'),active=nav?.querySelector('.tab.active');if(active){const n=nav.getBoundingClientRect(),a=active.getBoundingClientRect();if(a.left<n.left||a.right>n.right)nav.scrollLeft+=a.left-n.left-16;}for(const row of state.data.budget.filter(b=>b.confirmation)){const el=document.querySelector(`[data-budget-row="${CSS.escape(row.id)}"]`);if(el&&!el.querySelector('.comm-budget-source'))el.insertAdjacentHTML('beforeend',`<div class="comm-budget-source">${icon('check')}${t('Confirmed by')} ${esc(row.confirmation.recipient_name)} ${btn('View confirmation','budget-source','small ghost',`data-id="${esc(row.id)}"`)}</div>`);}if(highlight)document.querySelector(`[data-confirmation="${CSS.escape(highlight)}"]`)?.scrollIntoView({block:'nearest'});if((state.tab==='comments'&&!state.present)||state.communicationOpen){const visible=comments().filter(inThread);markCommentsRead(visible).catch(()=>{});}}
  function overview(){return enabled()?`<div class="comm-overview-note"><div>${icon('chat')}<span><strong>${t('Communication')}</strong><small>${pendingCount()} ${t('pending')} · ${t('Slide comments included')}</small></span></div>${btn('View pending confirmations','pending','small'+(pendingCount()?' has-pending-confirmations':''))}</div>`:'';}
  function clientPage(){return `<main class="main-content comm-client-page"><div class="section-title"><h1>${esc(state.data.project.name)}</h1>${btn('Back to presentation','back','','','arrow')}</div>${page()}</main>`;}
  document.addEventListener('click',async e=>{const el=e.target.closest('[data-action]');if(!el?.dataset.action.startsWith('comm-')||(!enabled()&&el.dataset.action!=='comm-location'))return;e.preventDefault();e.stopImmediatePropagation();const action=el.dataset.action.slice(5),id=el.dataset.id;if(busy)return;
    try{if(action==='location'){const project=el.dataset.project;if(project&&(!state.data||project!==state.data.project.id||el.dataset.iteration!==state.data.iteration.id)){closeModal();await openProject(project,el.dataset.iteration);}open(id);}if(action==='new')newConversation();if(action==='linked')newConversation('',null,thread);if(action==='edit-thread')editThread();if(action==='show')show();if(action==='back'){state.communicationOpen=false;render();}if(action==='pending'){view=state.client?'confirmations':'attention';show();}if(action==='view'){view=el.dataset.view;render();}if(action==='thread'){thread=id;replyTo=null;highlight=null;render();}if(action==='ask')popup(drafts.get(scope+thread)||'');if(action==='ask-existing')popup(comments().find(c=>c.id===id)?.body||'');if(action==='open')open(id);if(action==='reply'){open(id);replyTo=id;render();$('#comm-reply')?.focus();}if(action==='cancel-reply'){replyTo=null;render();}if(action==='source'){if(el.dataset.iteration!==state.data.iteration.id)await openProject(state.data.project.id,el.dataset.iteration);const n=slideDefs().findIndex(s=>s.id===el.dataset.slide);if(n>=0){state.communicationOpen=false;startPresentation(n);}}
      if(action==='revoke'){openModal(t('Remove conversation access?'),`<p>${t('This person will no longer be able to read or reply to this conversation or download its attachments.')}</p><div class="modal-footer">${button(t('Cancel'),'close-modal','ghost')}${btn('Remove access','do-revoke','primary',`data-id="${esc(id)}"`)}</div>`);}
      if(action==='do-revoke'){busy=true;await api('conversation_revoke',{id});closeModal();await refresh(true);toast(t('Access removed.'));}
      if(action==='withdraw'){const r=requests().find(r=>r.comment_id===id);openModal(t('Withdraw this request?'),`<p>${mentionBody(r.body,comments().find(c=>c.id===r.comment_id)?.mentions)}</p><p>${t('It will remain in the conversation as withdrawn. The budget will not change.')}</p><div class="modal-footer">${button(t('Cancel'),'close-modal','ghost')}${btn('Withdraw','do-withdraw','primary',`data-id="${esc(id)}"`)}</div>`);}
      if(action==='share'){openModal(t('Share conversation'),`<p>${t('Clients with access to the original iteration will be able to read all earlier messages and attachments. Studio-only action details stay private until you share those items.')}</p><div class="modal-footer">${button(t('Cancel'),'close-modal','ghost')}${btn('Share conversation','do-share','primary')}</div>`);}
      if(action==='do-share'){await api('communication_share',{iteration:contextIteration(),id:thread,share_history:true});closeModal();await refresh(true);}
      if(action==='work'){await api('communication_work_decide',{iteration:contextIteration(),id,resolved:el.dataset.resolved==='true'});await refresh(true);}
      if(action==='answered'){await api('comment_answered',{iteration:contextIteration(),id:thread,answered:!Number(root().answered)});await refresh(true);}
      if(action==='confirm'||action==='do-withdraw'){busy=true;el.disabled=true;await api('confirmation_decide',{iteration:requests().find(r=>r.comment_id===id)?.iteration_id||contextIteration(),id,decision:action==='confirm'?'confirmed':'withdrawn'});closeModal();await refresh(true);toast(t(action==='confirm'?'Confirmation recorded.':'Request withdrawn.'));}
      if(action==='budget-source')budgetSource(state.data.budget.find(b=>b.id===id));
      if(action==='budget'){const r=requests().find(r=>r.comment_id===id);if(r&&r.iteration_id!==state.data.iteration.id)await openProject(state.data.project.id,r.iteration_id);const row=state.data.budget.find(b=>b.confirmation?.comment_id===id);if(state.client){if(row)budgetSource(row);}else{state.tab='budget';render();}}
      if(action==='evidence'){if(el.dataset.iteration!==state.data.iteration.id)await openProject(state.data.project.id,el.dataset.iteration);const proxy=document.createElement('button');proxy.dataset.action=Number(el.dataset.page)?'legal-citation':'download';proxy.dataset.id=id;proxy.dataset.version=id;proxy.dataset.page=el.dataset.page;document.body.append(proxy);proxy.click();proxy.remove();}
      if(action==='download'){if(el.dataset.iteration&&el.dataset.iteration!==state.data.iteration.id)await openProject(state.data.project.id,el.dataset.iteration);const proxy=document.createElement('button');proxy.dataset.action='download';proxy.dataset.id=id;document.body.append(proxy);proxy.click();proxy.remove();}
    }catch(err){toast(err.message);}finally{busy=false;if(el.isConnected)el.disabled=false;}
  },true);
  document.addEventListener('input',e=>{if(e.target.id==='comm-reply')drafts.set(scope+thread,e.target.value);});
  document.addEventListener('change',async e=>{const el=e.target;
    if(el.matches('[data-comm-upload]')&&el.files[0]){const form=el.closest('form'),status=form.querySelector('[data-comm-upload-status]'),file=el.files[0],buttons=[...form.querySelectorAll('button')];if(file.size>100*1024*1024){status.textContent=t('Maximum file size: 100 MB.');return;}buttons.forEach(b=>b.disabled=true);form.dataset.uploading='1';status.textContent=t('Uploading…');try{const body=new FormData();body.set('iteration',form.dataset.iteration||contextIteration());body.append('files[]',file);const result=await api('communication_upload',body),select=form.querySelector('[name=version_id]');select.add(new Option(file.name+' · V1',result.ids[0]));select.value=result.ids[0];status.textContent=t('Linked · processing in the background');}catch(err){status.textContent=err.message;}finally{delete form.dataset.uploading;buttons.forEach(b=>b.disabled=false);}}
  });
  document.addEventListener('submit',async e=>{const form=e.target;if(!['comm-reply-form','comm-thread-form','comm-edit-thread-form'].includes(form.id))return;e.preventDefault();e.stopImmediatePropagation();if(busy||form.dataset.uploading)return;busy=true;const buttons=[...form.querySelectorAll('button')];buttons.forEach(b=>b.disabled=true);try{const values=Object.fromEntries(new FormData(form));if(values.thread_type!=='approval')delete values.checklist_id;const reply=replyTo&&comments().find(c=>c.id===replyTo),parent=form.id==='comm-thread-form'?'':reply?rootFor(reply).id:thread;const invite=!!(data()?.iteration_recipients?.[form.dataset.iteration]||recipients()).find(p=>p.email===values.recipient)?.invitable;if(form.id==='comm-edit-thread-form'){await api('communication_thread_update',{...values,iteration:contextIteration(),id:thread});closeModal();await refresh(true);return;}const result=await api('communication_post',{...values,...(values.annotation?{annotation:JSON.parse(values.annotation)}:{}),mentions:mentionData(form),invite,iteration:form.dataset.iteration||contextIteration(),parent_id:parent});if(form.id!=='comm-thread-form')drafts.delete(scope+thread);replyTo=null;if(form.id!=='comm-reply-form')closeModal();await refresh(true);if(form.id==='comm-thread-form'||invite||mentionData(form).some(m=>m.invite))open(result.id);toast(t(form.id==='comm-thread-form'?'Conversation started.':form.id==='comm-confirmation-form'?'Request sent.':'Reply posted.'));}catch(err){const error=form.querySelector('[data-comm-error]');if(error){error.hidden=false;error.textContent=err.message;}else toast(err.message);}finally{busy=false;buttons.forEach(b=>b.disabled=false);}},true);
  function badge(c){const r=c.confirmation;if(!r)return '';return `<div class="comm-confirmation-bottom"><span class="comm-status ${r.status==='confirmed'?'done':r.status==='withdrawn'?'closed':'pending'}">${t(r.status==='confirmed'?'Confirmed':r.status==='withdrawn'?'Withdrawn':'Pending confirmation')}${r.amount_cents!==null?' · '+signed(r.amount_cents):''}</span>${btn('View confirmation','location','small ghost',`data-id="${esc(c.id)}" data-project="${esc(c.project_id||state.data?.project.id||'')}" data-iteration="${esc(c.iteration_id)}"`)}</div>`;}
  setInterval(()=>{if(state.client&&state.communicationOpen&&!document.hidden&&!busy&&!document.querySelector('.modal')&&!document.activeElement?.closest('form'))refresh().catch(()=>{});},15000);
  function mentionContext(form){const reply=replyTo&&comments().find(c=>c.id===replyTo);return {iteration:form.dataset.iteration||contextIteration(),parent_id:form.id==='comm-thread-form'?'':reply?rootFor(reply).id:thread,audience:form.elements.audience?.value||'shared'};}
  return {feedback:(slide,annotation=null)=>{ensureScope();if(annotation){newConversation(slide,annotation);return;}const c=comments().find(c=>!c.parent_id&&c.slide===slide&&c.iteration_id===state.data.iteration.id);if(c)open(c.id);else newConversation(slide);},requestForChecklist:q=>{ensureScope();if(q.thread_id)thread=q.thread_id;replyTo=null;popup(q.question,q);},open,mentionContext,enabled,page,afterRender,overview,clientPage,budgetSource,badge,typeLabel,t};
}
